# Database Configuration
# ------------------
# PostgreSQL connection settings
POSTGRES_HOST=localhost        # Database host (default: localhost)
POSTGRES_PORT=5432             # Database port (default: 5432)
POSTGRES_DB=tododatabase       # Database name (default: tododatabase)

# Database Credentials (set by the user when creating the database)
# ------------------
# Replace these with secure credentials
POSTGRES_USER=user            # Database user
POSTGRES_PASSWORD=password    # Database password